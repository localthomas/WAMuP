{
  "name": "bbap",
  "version": "0.1.0",
  "description": "Browser Based Audio Player",
  "source": "src/index.html",
  "scripts": {
    "get-commit-hash": "printf \"GIT_COMMIT_HASH=%s\n\" \"$(git describe --always --broken --dirty --abbrev=128 --long)\" > .env.local",
    "licenses": "npx license-checker-rseidelsohn --customPath licenses-format.json --json --out licenses.json",
    "prerequisites": "npm run licenses && npm run get-commit-hash",
    "predev": "npm run prerequisites",
    "dev": "npx parcel",
    "check": "npx tsc --noEmit",
    "prebuild": "npm run prerequisites",
    "build": "npx parcel build --no-cache --log-level info"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.23",
    "@types/wicg-file-system-access": "^2020.9.5",
    "babel-preset-solid": "^1.3.13",
    "events": "^3.3.0",
    "license-checker-rseidelsohn": "^3.1.0",
    "parcel": "^2.4.0",
    "process": "^0.11.10",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "murmur-32": "^1.0.0",
    "music-metadata-browser": "^2.5.5",
    "solid-app-router": "^0.3.2",
    "solid-js": "^1.3.12"
  },
  "@parcel/bundler-default": {
    "minBundles": 999999999,
    "minBundleSize": 999999999,
    "maxParallelRequests": 1
  }
}